# 熙梦直播助手官网维护说明

## 本地预览

直接双击 `index.html` 即可浏览。也可以在本目录运行静态服务器：

```powershell
python -m http.server 4173 --bind 127.0.0.1
```

然后访问 `http://127.0.0.1:4173/`。

## 发布

这是纯静态网站，不需要 Node.js 构建。上传以下内容到任意静态网站空间即可：

- `index.html`
- `styles.css`
- `app.js`
- `assets/`
- `downloads/`

下载按钮指向 `downloads/熙梦直播助手-1.9.6-win.zip`。项目成品目录中的官网使用 NTFS 硬链接关联现有 ZIP，不会在本机重复占用一份磁盘空间。

## 更新版本

发布新版本时，只需：

1. 修改 `index.html` 中显示的版本号和下载文件名。
2. 将 `downloads/` 下的硬链接指向新的发布 ZIP。
3. 按需更新页面中的功能说明与系统要求。

官网公开内容只介绍正式发布的主程序功能。
